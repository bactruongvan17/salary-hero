export enum EmployeeType {
  DAILY = 'daily',
  MONTHY = 'monthy'
}

export enum WorkDuration {
  HALF_DAY = 'half_day',
  FULL_DAY = 'full_day'
}

export enum Currency {
  USD = 'usd',
  TBH = 'tbh'
}
